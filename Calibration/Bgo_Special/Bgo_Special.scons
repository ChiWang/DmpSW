
# invoke *scons in top-3 directory
print "\n\t\t--->building Calibration Bgo Special module\n"

Import('prefix')

# invoke top-3 directories' SConscript
subScript=[]
for key in ['Relation','Mip']:
    subScript=subScript+[key+'/'+key+'.scons']
SConscript(subScript,exports='prefix')

